input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  # Add filters here if needed to parse FastAPI router logs
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "fastapi-router-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
