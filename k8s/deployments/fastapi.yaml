# GATEWAY ROUTER
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastapi-router
  template:
    metadata:
      labels:
        app: fastapi-router
    spec:
      serviceAccountName: telemetry-service-account
      containers:
      - name: fastapi-router
        image: my-fastapi-router:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: ROLE
          value: "router"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
# TOXIC BASELINE WORKER
apiVersion: apps/v1
kind: Deployment
metadata:
  name: toxic-baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toxic-baseline
  template:
    metadata:
      labels:
        app: toxic-baseline
    spec:
      serviceAccountName: telemetry-service-account
      containers:
      - name: toxic-baseline
        image: my-fastapi-router:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: ROLE
          value: "worker"
        - name: MODEL_ID
          value: "martin-ha/toxic-comment-model"
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          periodSeconds: 2
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: toxic-baseline-service
spec:
  selector:
    app: toxic-baseline
  ports:
  - port: 8000
    targetPort: 8000
---
# TOXIC BERT WORKER
apiVersion: apps/v1
kind: Deployment
metadata:
  name: toxic-bert
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toxic-bert
  template:
    metadata:
      labels:
        app: toxic-bert
    spec:
      serviceAccountName: telemetry-service-account
      containers:
      - name: toxic-bert
        image: my-fastapi-router:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: ROLE
          value: "worker"
        - name: MODEL_ID
          value: "unitary/toxic-bert"
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          periodSeconds: 2
        resources:
          requests:
            cpu: "300m"
            memory: "768Mi"
          limits:
            cpu: "1"
            memory: "1.5Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: toxic-bert-service
spec:
  selector:
    app: toxic-bert
  ports:
  - port: 8000
    targetPort: 8000
---
# TOXIC ROBERTA WORKER
apiVersion: apps/v1
kind: Deployment
metadata:
  name: toxic-roberta
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toxic-roberta
  template:
    metadata:
      labels:
        app: toxic-roberta
    spec:
      serviceAccountName: telemetry-service-account
      containers:
      - name: toxic-roberta
        image: my-fastapi-router:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: ROLE
          value: "worker"
        - name: MODEL_ID
          value: "unitary/unbiased-toxic-roberta"
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          periodSeconds: 2
        resources:
          requests:
            cpu: "300m"
            memory: "768Mi"
          limits:
            cpu: "1"
            memory: "1.5Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: toxic-roberta-service
spec:
  selector:
    app: toxic-roberta
  ports:
  - port: 8000
    targetPort: 8000